pydantic>=2.0
pydantic-settings>=2.0
pytest>=8.0
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27  # used by tests via FastAPI's TestClient
anthropic>=0.40  # used by app/core/ai_provider.py's AnthropicAIProvider; lazily imported, not required for tests
